[server]
host = "0.0.0.0"
port = 9090

# Provider configurations (pre-registered providers)
# Dynamic registration is now available via POST /register

[[providers]]
id = "provider-1"
name = "Test GPU Provider"
base_url = "http://localhost:8080"
api_key = "${PROVIDER_API_KEY}"
